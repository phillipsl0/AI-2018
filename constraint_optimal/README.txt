*ConstraintOptimalHeatMiser.py*

To run our code:

1. In terminal, go to the directory with ConstraintOptimalHeatMiser.py
2. Run the program by entering (without the quotation marks): "pipenv run python ConstraintOptimalHeatMiser.py"
    a. if you run into the following error: "RuntimeError: Python is not installed as a framework."
        -> echo "backend: TkAgg" >> ~/.matplotlib/matplotlibrc
3. Follow the printed prompts.

